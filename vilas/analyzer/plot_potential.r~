residues <- read.table("cutoff-resid-14-angstroms",header=F)
for(res in residues){

file <- read.table(res,header=TRUE) ## if you have headers in your files ##
meanCoul <- mean(file$CoulSRresidRNA)
meanLJ <- mean(file$LJSRresidRNA)

write.table(mean,file=paste(paste("mean",filename[i],sep="."),sep="/")) 
##if you wish to write the means of all the files in seperate files rather than one.
}
